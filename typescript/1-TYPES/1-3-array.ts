{
    const fruits: string[] = ['🍎', '🍌'];
    const scores: number[] = [1, 3, 4];

    function printArray(fruits: readonly string[]) {
        // fruits.push(); // Error
    }

    // Tuple
    let student : [string, number];
    student = ['name', 123];
    student[0] // name
    
}